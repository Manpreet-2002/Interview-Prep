#include <bits/stdc++.h>
using namespace std;

vector<int> solve(vector<int> arr, int k)
{
    int size = arr.size();
    int i = 0;
    j = 0;

    vector<int> ans;
    int buf = 0;
    while (j < size)
    {
    }
}

int main()
{
    return 0;
}